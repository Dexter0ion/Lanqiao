#include<iostream>
#include<stdio.h> 
#include<string.h>
#define maxn 101
using namespace std;

int numerator[maxn];//·Ö×Ó
int denominator[maxn];//·ÖÄ¸

int gcd()
int main()
{
	memset(numerator,0,sizeof(numerator));
	memset(denominator,0,sizeof(denominator));
	int N;
	cin>>N;
	for(int i=0;i<N;i++)
	{
		scanf("%d/%d",&numerator[i],&denominator);
	}
}
